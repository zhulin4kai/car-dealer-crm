# 项目协作总览

本文件是 AI 开发工具进入项目后的首要入口。开始任务前，先读取本文件，再按任务范围读取 `docs/` 中的规范。

## 项目结构

- `dealer-server/`：Spring Boot、MyBatis、MariaDB/MySQL、Redis 后端。
- `dealer-web/`：Vue 3、TypeScript、Vite 前端。
- `docs/`：业务规格、方针计划、执行任务、架构、接口、规则和协作说明。

## 文档职责

### 顶层文档

- `docs/api/openapi.yaml`：前后端 API 路径、参数、请求体、响应体和错误响应契约。
- `docs/api/api-conventions.md`：API 路径、分页、响应、错误码、时间格式和兼容规则。
- `docs/backend.md`：后端分层、模块和基础设施说明。
- `docs/frontend.md`：前端目录、状态管理和页面组织说明。
- `docs/integration.md`：前后端联调、认证和环境协作说明。

### 子目录

- `docs/spec/`：业务规格，只描述业务愿景、目标用户、范围、核心能力、非目标和成功标准。
- `docs/plan/`：方针计划，说明现状、目标方案、风险、实施阶段和验收标准。
- `docs/task/`：具体执行任务，说明文件范围、代码落点、执行步骤、验证命令和交付说明。
- `docs/rule/`：长期有效的开发、代码、测试、Git、工具和交付规范。

## 阅读顺序

1. `AGENTS.md`。
2. `docs/rule/00-规则总览与优先级.md`。
3. 与任务相关的业务 Spec。
4. 与任务相关的 Plan。
5. 与任务相关的 Task。
6. 与任务相关的技术规则和顶层文档。

## 基本要求

- 标识符使用英文，文档、注释和提交摘要使用中文。
- 修改前读取现有实现和测试，不凭空假设。
- 业务正确性、安全和数据完整性优先于代码风格。
- 后端行为变化必须检查前端契约，前端变化不得绕过后端规则。
- 修复必须包含相应验证，不得通过降低断言或退化行为换取测试通过。
- 未经明确要求，不启动服务、不安装依赖、不提交 Git、不扩大修改范围。
- 不使用 graphify；代码定义、调用关系和影响面优先使用 `codebase-memory-mcp`。
- 字符串、配置和非代码文件检索使用 `rg`、`rg --files`，知识图谱结果不足时也可回退使用。

## 冲突处理

单次任务说明决定当前工作的范围、操作方式和验收要求。它与业务 Spec、安全约束或数据契约冲突时，必须先指出冲突并等待确认，不得自行扩大范围或静默放宽约束。
