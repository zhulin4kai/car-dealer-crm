<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autodealer.crm.mapper.TProductPromotionMapper">
    
    <resultMap id="BaseResultMap" type="com.autodealer.crm.model.TProductPromotion">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="product_id" property="productId" />
        <result column="type" property="type" />
        <result column="discount" property="discount" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <sql id="Base_Column_List">
        id, product_id, name, type, discount, start_time, end_time, status, create_time, update_time
    </sql>
    
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_product_promotion
        ORDER BY create_time DESC
    </select>
    
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM t_product_promotion
    </select>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_product_promotion
        WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.autodealer.crm.model.TProductPromotion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_product_promotion (
            name, type, discount, start_time, end_time, status, create_time, update_time
        ) VALUES (
            #{name}, #{type}, #{discount}, #{startTime}, #{endTime}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>
    
    <update id="update" parameterType="com.autodealer.crm.model.TProductPromotion">
        UPDATE t_product_promotion
        SET name = #{name},
            type = #{type},
            discount = #{discount},
            start_time = #{startTime},
            end_time = #{endTime},
            status = #{status},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM t_product_promotion
        WHERE id = #{id}
    </delete>
</mapper> 