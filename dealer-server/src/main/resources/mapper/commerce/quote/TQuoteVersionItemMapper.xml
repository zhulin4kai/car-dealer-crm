<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autodealer.crm.modules.commerce.quote.persistence.mapper.TQuoteVersionItemMapper">
    <resultMap id="BaseResultMap" type="com.autodealer.crm.modules.commerce.quote.application.api.model.TQuoteVersionItem">
        <id column="id" property="id" />
        <result column="quote_version_id" property="quoteVersionId" />
        <result column="product_id" property="productId" />
        <result column="product_sku" property="productSku" />
        <result column="product_name" property="productName" />
        <result column="product_specification" property="productSpecification" />
        <result column="guide_price" property="guidePrice" />
        <result column="unit_price" property="unitPrice" />
        <result column="quantity" property="quantity" />
        <result column="line_amount" property="lineAmount" />
        <result column="promotion_id" property="promotionId" />
        <result column="promotion_code" property="promotionCode" />
        <result column="promotion_name" property="promotionName" />
        <result column="promotion_rule_summary" property="promotionRuleSummary" />
        <result column="promotion_amount" property="promotionAmount" />
        <result column="promotion_snapshot" property="promotionSnapshot" />
        <result column="create_time" property="createTime" />
        <result column="create_by" property="createBy" />
    </resultMap>

    <sql id="Base_Column_List">
        id, quote_version_id, product_id, product_sku, product_name, product_specification,
        guide_price, unit_price, quantity, line_amount, promotion_id, promotion_code,
        promotion_name, promotion_rule_summary, promotion_amount, promotion_snapshot,
        create_time, create_by
    </sql>

    <select id="selectByVersionId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_quote_version_item
        WHERE quote_version_id = #{quoteVersionId}
        ORDER BY id ASC
    </select>

    <insert id="insert" parameterType="com.autodealer.crm.modules.commerce.quote.application.api.model.TQuoteVersionItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_quote_version_item (
            quote_version_id, product_id, product_sku, product_name, product_specification,
            guide_price, unit_price, quantity, line_amount, promotion_id, promotion_code,
            promotion_name, promotion_rule_summary, promotion_amount, promotion_snapshot,
            create_time, create_by
        ) VALUES (
            #{quoteVersionId}, #{productId}, #{productSku}, #{productName}, #{productSpecification},
            #{guidePrice}, #{unitPrice}, #{quantity}, #{lineAmount}, #{promotionId}, #{promotionCode},
            #{promotionName}, #{promotionRuleSummary}, #{promotionAmount}, #{promotionSnapshot},
            #{createTime}, #{createBy}
        )
    </insert>

    <delete id="deleteByVersionId">
        DELETE FROM t_quote_version_item
        WHERE quote_version_id = #{quoteVersionId}
    </delete>
</mapper>
