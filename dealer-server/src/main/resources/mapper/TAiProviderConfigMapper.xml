<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autodealer.crm.ai.mapper.TAiProviderConfigMapper">
    <resultMap id="BaseResultMap" type="com.autodealer.crm.ai.model.TAiProviderConfig">
        <id column="id" property="id" />
        <result column="config_no" property="configNo" />
        <result column="provider_name" property="providerName" />
        <result column="provider_format" property="providerFormat" />
        <result column="base_url" property="baseUrl" />
        <result column="model_name" property="modelName" />
        <result column="model_display_name" property="modelDisplayName" />
        <result column="encrypted_api_key" property="encryptedApiKey" />
        <result column="api_key_nonce" property="apiKeyNonce" />
        <result column="masked_api_key" property="maskedApiKey" />
        <result column="enabled" property="enabled" />
        <result column="test_status" property="testStatus" />
        <result column="last_test_time" property="lastTestTime" />
        <result column="last_test_error_code" property="lastTestErrorCode" />
        <result column="last_test_message" property="lastTestMessage" />
        <result column="timeout_seconds" property="timeoutSeconds" />
        <result column="max_output_tokens" property="maxOutputTokens" />
        <result column="temperature" property="temperature" />
        <result column="create_time" property="createTime" />
        <result column="create_by" property="createBy" />
        <result column="edit_time" property="editTime" />
        <result column="edit_by" property="editBy" />
    </resultMap>

    <sql id="Base_Column_List">
        id, config_no, provider_name, provider_format, base_url, model_name, model_display_name,
        encrypted_api_key, api_key_nonce, masked_api_key, enabled, test_status, last_test_time,
        last_test_error_code, last_test_message, timeout_seconds, max_output_tokens,
        temperature, create_time, create_by, edit_time, edit_by
    </sql>

    <insert id="insert" parameterType="com.autodealer.crm.ai.model.TAiProviderConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_ai_provider_config
        (config_no, provider_name, provider_format, base_url, model_name, model_display_name,
         encrypted_api_key, api_key_nonce, masked_api_key, enabled, test_status, last_test_time,
         last_test_error_code, last_test_message, timeout_seconds, max_output_tokens,
         temperature, create_time, create_by, edit_time, edit_by)
        VALUES
        (#{configNo}, #{providerName}, #{providerFormat}, #{baseUrl}, #{modelName}, #{modelDisplayName},
         #{encryptedApiKey}, #{apiKeyNonce}, #{maskedApiKey}, #{enabled}, #{testStatus}, #{lastTestTime},
         #{lastTestErrorCode}, #{lastTestMessage}, #{timeoutSeconds}, #{maxOutputTokens},
         #{temperature}, #{createTime}, #{createBy}, #{editTime}, #{editBy})
    </insert>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_ai_provider_config
        ORDER BY create_time DESC, id DESC
    </select>

    <select id="selectByConfigNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_ai_provider_config
        WHERE config_no = #{configNo}
    </select>

    <select id="selectByConfigNoForUpdate" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_ai_provider_config
        WHERE config_no = #{configNo}
        FOR UPDATE
    </select>

    <select id="selectEnabled" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM t_ai_provider_config
        WHERE enabled = TRUE
        ORDER BY edit_time DESC, id DESC
        LIMIT 1
    </select>

    <update id="updateBaseFields" parameterType="com.autodealer.crm.ai.model.TAiProviderConfig">
        UPDATE t_ai_provider_config
        SET provider_name = #{providerName},
            provider_format = #{providerFormat},
            base_url = #{baseUrl},
            model_name = #{modelName},
            model_display_name = #{modelDisplayName},
            timeout_seconds = #{timeoutSeconds},
            max_output_tokens = #{maxOutputTokens},
            temperature = #{temperature},
            test_status = 'UNTESTED',
            last_test_error_code = NULL,
            last_test_message = NULL,
            edit_time = CURRENT_TIMESTAMP,
            edit_by = #{editBy}
        WHERE id = #{id}
    </update>

    <update id="updateApiKey">
        UPDATE t_ai_provider_config
        SET encrypted_api_key = #{encryptedApiKey},
            api_key_nonce = #{apiKeyNonce},
            masked_api_key = #{maskedApiKey},
            test_status = 'UNTESTED',
            last_test_error_code = NULL,
            last_test_message = NULL,
            edit_time = CURRENT_TIMESTAMP,
            edit_by = #{editBy}
        WHERE id = #{id}
    </update>

    <update id="disableAll">
        UPDATE t_ai_provider_config
        SET enabled = FALSE,
            edit_time = CURRENT_TIMESTAMP,
            edit_by = #{editBy}
        WHERE enabled = TRUE
    </update>

    <update id="updateEnabled">
        UPDATE t_ai_provider_config
        SET enabled = #{enabled},
            edit_time = CURRENT_TIMESTAMP,
            edit_by = #{editBy}
        WHERE id = #{id}
    </update>

    <update id="updateTestResult">
        UPDATE t_ai_provider_config
        SET test_status = #{testStatus},
            last_test_time = CURRENT_TIMESTAMP,
            last_test_error_code = #{lastTestErrorCode},
            last_test_message = #{lastTestMessage},
            edit_time = CURRENT_TIMESTAMP,
            edit_by = #{editBy}
        WHERE id = #{id}
    </update>
</mapper>
