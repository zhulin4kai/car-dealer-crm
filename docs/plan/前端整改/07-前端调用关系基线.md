# 前端调用关系基线

本文件记录本轮通过 codebase-memory-mcp 和源码交叉核对得到的结构基线，供整改时做影响面检查，不代替 API 文档。

## 应用主链路

```text
main.ts
  -> Pinia
  -> v-has-permission
  -> Vue Router
      -> guards.ts
          -> auth.store.ts -> user-api.ts -> /api/login/info
          -> permission.store.ts -> permission-storage.ts
      -> DashboardLayout.vue
          -> menuPermissionList
          -> pages/dashboard/**
              -> modules/*/api/*-api.ts
                  -> shared/api/http-client.ts
                      -> dealer-server Controller
```

认证事实来源当前分为：Token storage、Auth Store 内存状态、Permission storage、Permission Store 内存菜单/功能码和后端 Redis/数据库会话。整改时必须保持后端会话为权威，前端只缓存并可完整清理。

## 路由、页面权限和主要接口

| 路由 | 页面权限 | 页面 | 主要后端接口 |
|---|---|---|---|
| `/dashboard` | `statistic:view` | `dashboard/index.vue` | `/api/summary/data`、`/api/saleFunnel/data`、`/api/sourcePie/data` |
| `/dashboard/user` | `user:list` | `dashboard/user.vue` | `/api/users`、`/api/user*` |
| `/dashboard/activity` | `activity:list` | `activity/index.vue` | `/api/activitys`、`/api/activity*` |
| `/dashboard/activity/:id` | `activity:view` | `activity/[id].vue` | `/api/activity/{id}`、`/api/activity/remark*` |
| `/dashboard/clue` | `clue:list` | `clue/index.vue` | `/api/clues`、`/api/clue*`、`/api/importExcel` |
| `/dashboard/clue/detail/:id` | `clue:view` | `clue/detail/[id].vue` | `/api/clue/detail/{id}`、`/api/clue/remark`、`/api/clue/customer` |
| `/dashboard/customer` | `customer:list` | `dashboard/customer.vue` | `/api/customer/list`、`/api/exportExcel` |
| `/dashboard/product` | `product:list` | `product/index.vue` | `/api/products`、`/api/product-categories` |
| `/dashboard/product/category` | `product:category:list` | `product/category.vue` | `/api/product-categories` |
| `/dashboard/product/promotion` | `product:promotion:list` | `product/promotion.vue` | `/api/product-promotions` |
| `/dashboard/product/stock` | `product:stock:view` | `product/stock.vue` | `/api/products/stockalerts`、`/api/productstock/*` |
| `/dashboard/tran` | `tran:list` | `tran/index.vue` | `/api/tran/list`、`/api/tran/create|update|batch-delete` |
| `/dashboard/tran/:id` | `tran:view` | `tran/[id].vue` | 交易详情、商品、发票、支付、结算、退款 |
| `/dashboard/tran/approve/:id` | `tran:approve` | `tran/approve/[id].vue` | 交易详情/商品（需 `tran:view`）、审批（需 `tran:approve`） |
| `/dashboard/tran/invoice/:id` | `tran:invoice` | `tran/invoice/[id].vue` | 交易详情/商品/发票列表（需 `tran:view`）、发票命令（需 `tran:invoice`） |
| `/dashboard/dict/type` | `dict:type:list` | `dict/type.vue` | `/api/dict/types`、`/api/dict/type/*` |
| `/dashboard/dict/value` | `dict:value:list` | `dict/value.vue` | `/api/dict/values`、`/api/dict/value/*` |
| `/dashboard/system` | `system:list` | `dashboard/system.vue` | `/api/system/*`，以及独立要求 `monitor:view` 的 `/api/monitor/*` |

## Store 与权限调用关系

- `auth.store.ts`：负责 Token、rememberMe、当前用户、登录/退出和会话恢复；调用 `user-api.ts`。
- `permission.store.ts`：负责功能权限码和菜单树；既可从 sessionStorage 恢复功能码，也可通过 `/api/login/info` 重新加载完整权限。
- `guards.ts`：进入受限路由前恢复 Token/权限；菜单树为空时拉取当前用户；按 `route.meta.permission` 判定页面权限。
- `has-permission.ts`：按 Permission Store 功能码移除无权 DOM，只用于体验控制，不承担后端授权。
- `DashboardLayout.vue`：直接消费后端 `menuPermissionList` 生成菜单；退出时同时清理 Auth Store 和 Permission Store。

## 模块 API 与后端 Controller 边界

| 前端模块 | API 文件 | 后端 Controller |
|---|---|---|
| activity | `modules/activity/api/activity-api.ts` | `ActivityController`、`ActivityRemarkController` |
| clue/customer | `modules/clue/api/clue-api.ts`、`modules/customer/api/customer-api.ts` | `ClueController`、`ClueRemarkController`、`CustomerController` |
| product | `modules/product/api/product-api.ts` | `ProductController`、`ProductCategoryController`、`ProductPromotionController`、`ProductStockController` |
| tran | `modules/tran/api/tran-api.ts` | `TranController` |
| user | `modules/user/api/user-api.ts` | `UserController`、Spring Security 登录/退出处理器 |
| dict | `modules/dict/api/dict-api.ts` | `DicController` |
| system | `modules/system/api/system-api.ts` | `SystemController`、`SystemMonitorController` |
| statistic | `modules/statistic/api/statistic-api.ts` | `StatisticController` |

## 整改影响面规则

- 修改页面按钮权限时，同时核对路由 meta、`PERMISSIONS`、后端 `PermissionCodes`、Controller `@PreAuthorize` 和初始化角色权限。
- 修改 API 请求时，同时核对模块类型、Controller 方法、Request DTO、Response DTO/PageInfo 和错误码。
- 修改交易操作时，同时核对 `TranStage`、`TranServiceImpl` 原子状态迁移、库存、发票、支付和交易历史。
- 修改退出或失效处理时，同时清理 Token、currentUser、permissionList、menuPermissionList 和持久化缓存。
