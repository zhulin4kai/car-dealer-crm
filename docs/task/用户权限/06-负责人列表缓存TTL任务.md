# 负责人列表缓存 TTL 任务

## 关联审计问题
- P1-14

## 权威依据
- Spec：docs/spec/用户权限/00-业务范围与边界.md
- Spec：docs/spec/用户权限/01-流程规则与验收规格.md
- Plan：docs/plan/用户权限/01-用户权限与数据范围治理方案.md
- Rule：docs/rule/01-开发流程与架构边界.md
- Rule：docs/rule/05-API认证与错误响应规范.md
- Rule：docs/rule/06-业务一致性与事务规范.md
- Rule：docs/rule/07-测试编写执行与验收规范.md

## 当前错误行为
- 当前负责人列表缓存无 TTL，Redis List 并发 miss 可能重复回填。

## 目标不变量
- 负责人候选缓存必须有 TTL，且并发回填不产生重复项。

## 前置依赖
- 无前置依赖。

## 允许修改范围
- `dealer-server` 中本任务直接涉及的 Controller、Service、Mapper、DTO、Model、枚举和测试。
- `dealer-web` 中本任务直接涉及的页面、API 模块、类型、枚举映射和测试。
- 本任务必须同步的数据库迁移、OpenAPI、错误码和文档引用。

## 禁止修改范围
- 不得修改其他业务域负责关闭的主 Task。
- 不得通过删除历史事实、降低断言、放宽权限或吞掉异常让验证通过。
- 不得把多个业务动作重新压缩进一个通用 `stage`、中文字符串或布尔字段。
- 不得绕过后端规则只在前端隐藏入口。

## 契约变化
- API：按现有接口保持兼容；如新增或调整接口，必须同步 OpenAPI。
- DTO：按任务涉及的请求、响应对象补齐字段和校验。
- CodeEnum：涉及状态、类型、错误码时使用稳定英文 code。
- 数据库：涉及唯一性、外键、流水和历史事实时补齐迁移与兼容方案。
- 前端类型：涉及前端页面时同步 TypeScript 类型、枚举映射和错误码处理。

## 执行步骤
1. 定位 `UserServiceImpl#getOwnerList` 和 `RedisManager#setList` 使用点。
2. 负责人候选只返回启用、未锁定、具备销售负责人资格的账号。
3. 缓存值改为单 value 序列化列表，不使用非原子 List 追加回填。
4. 设置明确 TTL。
5. 用户禁用、角色变化、部门变化、负责人资格变化时失效缓存。
6. Redis 删除失败可观测并返回失败或触发重试。
7. 补充双并发 miss、禁用用户不出现、TTL 生效、失效失败测试。

## 测试
- 原问题回归：按关联审计问题或业务缺口构造失败前用例。
- 越权：涉及客户、交易、资金、票据、库存、导出时覆盖无权限路径。
- 重复请求：涉及创建、确认、退款、占用、导入、转化时覆盖幂等或冲突。
- 并发：涉及唯一业务键、库存、收款、退款、预算时覆盖并发竞争。
- 事务回滚：涉及多表写入时证明失败后无半成品。
- 契约：涉及接口或前端类型时同步 OpenAPI、DTO 和 TypeScript 类型。

## 验证命令
- `cd dealer-server && ./mvnw -DskipTests compile`
- `cd dealer-web && npm run typecheck`
- `git diff --check`

## 完成证据
- 列出实际修改的类、方法、Mapper、前端模块和数据库迁移。
- 列出新增或更新的测试名称及执行结果。
- 列出 OpenAPI、前端类型和错误码同步结果。
- 说明本任务依赖的其他 Task 是否已完成，未完成时不得宣称关闭上游审计问题。
