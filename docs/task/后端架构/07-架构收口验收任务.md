# 架构收口验收任务

## 静态验收

- 生产根目录只保留 `bootstrap`、`shared`、`modules`。
- 旧 `web/service/mapper/model/dto/query/result/ai/config` 生产目录不存在。
- 不存在旧包转发类、重复接口、重复模型、旧 Bean 或临时迁移脚本。
- Mapper 配置统一使用 `classpath*:mapper/**/*.xml`。
- `docs/backend.md` 与 Plan 描述实际模块结构。

## 构建验收

```bash
cd dealer-server
./mvnw clean test
```

结果：构建成功；测试 1039 项，失败 0、错误 0、跳过 4。

## 行为边界

本次重构未修改 HTTP 契约、OpenAPI、数据库 Schema、SQL 语义、权限码、稳定错误码、
状态机、事务、缓存键或前端代码。
